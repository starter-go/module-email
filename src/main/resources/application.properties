
[mails]
    dispatcher-name-list = mock,sms,smtp
    default-sender-address=mock@example.com

[mails "mock"]
    driver = mock
    sender-address = mock@example.com
    sender-name = Mocker
    host=a.b.c
    port=0
    password=123
    username=123
    workaround=0
    priority=0
    enabled=1

[mails "smtp"]
    driver = smtp
    sender-address = mock@example.com (todo)
    sender-name = Test
    host= smtp.qiye.aliyun.com
    port= 465
    password=123 (todo)
    username=123 (todo)
    workaround=1
    priority=0
    enabled=1

[mails "sms"]
    driver = sms
    sender-address = sms@example.com
    sender-name = sms
    host= sms.demo
    port= 0
    password=123 (todo)
    username=123 (todo)
    workaround=1
    priority=0
    enabled=1
